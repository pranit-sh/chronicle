{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build extension",
      "type": "shell",
      "command": "pnpm --filter @codicil/core build && pnpm --filter codicil-vscode build",
      "group": "build",
      "problemMatcher": "$tsc"
    }
  ]
}
