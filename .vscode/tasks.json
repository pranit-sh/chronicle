{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build extension",
      "type": "shell",
      "command": "pnpm --filter @chronicle/core build && pnpm --filter chronicle-vscode build",
      "group": "build",
      "problemMatcher": "$tsc"
    }
  ]
}
