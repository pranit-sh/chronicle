# The extension ships as a single bundled file, so nothing else is needed.
src/**
test/**
tsconfig.json
tsup.config.ts
node_modules/**
**/*.map
**/*.ts

# The marketplace reads the rendered PNG; the source drawing stays in git only.
media/icon.svg

.vscodeignore
*.vsix
